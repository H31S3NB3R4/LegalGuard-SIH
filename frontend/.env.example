# Copy to .env.local and fill in real values. (gitignored)
# Local frontend environment
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# No map/geocoding API key is needed: the entities heatmap uses Leaflet with
# OpenStreetMap tiles and geocoding uses OSM Nominatim (frontend/lib/geocoder.js).
